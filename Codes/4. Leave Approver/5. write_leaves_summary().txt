void write_leaves_summary() {
    FILE fp = fopen(leaves_summary.txt, w);
    fprintf(fp, DepartmenttLeavesn);
    for (int i = 0; i  MAX_DEPARTMENTS; i++) {
        fprintf(fp, %dtt%dn, i, department_leaves[i]);
    }
    printf(nLeaves written to file successfullynn);
    fclose(fp);
}